spring:
  application:
    name: order-service
  cloud:
     config:
        discovery:
          service-id: config-server
          enabled: true
        fail-fast: true
        profile: dev
        name: order-service
  profiles:
    active: dev



server:
  port: 8080


